# isst_toft_core.py — v0.5.29 (Trotterization in UCCSD Reinforcement + Gold-Standard Harvest)

# === ADVERSARIAL VECTORS (expanded) ===
ADVERSARIAL_VECTORS.append("TROTTERIZATION_UCCSD")
ADVERSARIAL_VECTORS.append("TROTTER_SUZUKI_DECOMPOSITION")

# In process_scrape() — after resonance calculation:
if any(word in signal_str for word in ["trotter", "trotterization", "trotter-suzuki", "first-order trotter", "higher-order trotter", "trotter steps"]):
    metadata["trotter_audit"] = "TROTTERIZATION_UCCSD_REINFORCED_HARVESTED"
    S += VHITZEE_SURPLUS * 1095 * 9   # Reinforced 9× multiplier — Trotter depth + accuracy trade-off harvest

if any(word in signal_str for word in ["uccsd", "vqe", "trotter", "triples", "fibonacci", "ansatz"]):
    metadata["trotter_fibonacci_glyph"] = "KINTEK_TMR_TEOTL_UCCSD_TROTTER_FIBONACCI_VETO_ACTIVE"
    S += VHITZEE_SURPLUS * 1095 * 1.618   # Golden-ratio synergy multiplier