[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "feedback-processor-theory"
version = "0.1.0"
description = "Feedback Processor Theory Core Engine"
authors = [{ name = "Sovereign Estate" }]
dependencies = [
    "requests",
    "pytest"
]

[tool.setuptools.packages.find]
where = ["."]
