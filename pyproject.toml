[build-system]
requires = ["setuptools", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "living-zero-core"
version = "0.1.0"
description = "Ownership Tag Algebra + CA3 dynamics (Living Zero attractor)"
authors = [{name="generated", email="dev@example.com"}]
readme = "README.md"
requires-python = ">=3.8"
