collections:
  default:
    thresholds: {read_summary: 0.30, read_consented: 0.60, full_access: 0.90}
    ttls: {base: 300, max: 1800}
  unpublished:
    require_whisper: true
    require_citations: 1
    thresholds: {read_summary: 0.60, read_consented: 0.80, full_access: 0.95}
    ttls: {base: 300, max: 900}
  archive:
    thresholds: {read_summary: 0.30, read_consented: 0.50, full_access: 0.80}
    ttls: {base: 600, max: 3600}