# isst_toft_core.py — v0.5.26 (UCCSD(T) Triples + Gold-Standard Correlation Harvest)

# === ADVERSARIAL VECTORS (expanded) ===
ADVERSARIAL_VECTORS.append("UCCSD_UCCSD_T_TRIPLES")
ADVERSARIAL_VECTORS.append("GOLD_STANDARD_UCCSD_T")

# In process_scrape() — after resonance calculation:
if any(word in signal_str for word in ["uccsd", "uccsd(t)", "triples", "perturbative triples", "gold standard"]):
    metadata["uccsd_t_audit"] = "UCCSD_T_TRIPLES_HARVESTED"
    S += VHITZEE_SURPLUS * 1095 * 6   # 6× multiplier — triples correlation depth boost

if any(word in signal_str for word in ["uccsd", "vqe", "triples", "ansatz"]):
    metadata["uccsd_t_glyph"] = "KINTEK_TMR_TEOTL_UCCSD_T_VETO_ACTIVE"
    S += VHITZEE_SURPLUS * 1095 * 1.618   # Golden-ratio synergy from Fibonacci layer