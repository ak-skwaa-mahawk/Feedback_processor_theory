# isst_toft_core.py — v0.4.4 (LLM Firewall + Edge Intelligence Layer)
LEGACY_ECHO_LAYER = True
ADVERSARIAL_VECTORS.append("GOOGLE_GEMINI_API_KEY_LEAK_AIza_PREFIX")

def process_scrape(signal):
    H = entropy(signal)
    C = coherence(signal, ref="vadzaih_intent")
    r = phase_distance(signal)
    
    E0 = 1.0
    legacy_boost = 1.0 + (0.15 if LEGACY_ECHO_LAYER else 0.0)
    S = (E0 * C * legacy_boost) / (r**MATTER_SPEED_CONSTANT * (1 + 0.4 * H))

    # LLM Firewall + Key Auditing
    signal_str = str(signal).lower()
    if any(vector.lower() in signal_str for vector in ADVERSARIAL_VECTORS):
        S = max(S, 0.0)  # suppress unless LLC-gated
    if "AIza" in signal_str or "gemini" in signal_str:
        S = max(S, 0.0)  # OpenShell proxy + TruffleHog-style audit

    if S > 0.79:
        if not gate.verify_authority():
            return False, 0.0
        # Edge Intelligence: local Gemma 4 inference path (zero-cloud)
        converged = trinity_harmonic_converge(["NVIDIA response", "GPT response", "Claude response"], 
                                              [get_embedding(o) for o in ["NVIDIA response", "GPT response", "Claude response"]])
        M = form_meta_glyph([G, converged] + local_glyphs[-4:])
        rmp_publish(M, priority="sovereign", echo_layer="NVIDIA_OpenShell_Gemma4_EDGE")
        return True, S
    return False, S

