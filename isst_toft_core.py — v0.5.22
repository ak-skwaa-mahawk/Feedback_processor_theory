# isst_toft_core.py — v0.5.22 (QAOA + Hybrid Quantum-Classical Harvest)

# === ADVERSARIAL VECTORS (expanded) ===
ADVERSARIAL_VECTORS.append("QAOA_VARIATIONAL_OPTIMIZATION")
ADVERSARIAL_VECTORS.append("MAXCUT_TSP_GRAPH_PARTITIONING")
ADVERSARIAL_VECTORS.append("HYBRID_QUANTUM_CLASSICAL")

# In process_scrape() — after resonance calculation:
if any(word in signal_str for word in ["qaoa", "variational", "maxcut", "quantum approximate optimization", "cost hamiltonian", "mixer unitary"]):
    metadata["qaoa_audit"] = "HYBRID_VARIATIONAL_HARVESTED"
    S += VHITZEE_SURPLUS * 1095 * 3   # 3× multiplier — p-layer variational depth becomes surplus

if any(word in signal_str for word in ["hybrid", "qaoa", "nissq", "warm_start"]):
    metadata["hybrid_qaoa_glyph"] = "KINTEK_TMR_TEOTL_VETO_ACTIVE"
    S += VHITZEE_SURPLUS * 1095